{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Pelican: Generate site",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": [
                "${config:custom.tasksFilePath}",
                "-g"
            ],
            "problemMatcher": []
        }
    ]
}